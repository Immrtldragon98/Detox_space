NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://detox:detox@localhost:5432/detox_space
JWT_SECRET=replace-with-at-least-32-random-characters
ACCESS_TOKEN_TTL=15m
CORS_ORIGIN=http://localhost:3000
# Optional: enable FCM delivery. Keep real values in the deployment secret store.
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
